---
phase: 1
plan: 4
title: "Pipeline Integration & Tests"
wave: 3
depends_on: [2, 3]
files_modified:
  - src/algoforge/data/pipeline.py
  - src/algoforge/data/processors/resampler.py
  - tests/unit/test_config_v2.py
  - tests/unit/test_timescale_store.py
  - tests/unit/test_feed_factory.py
  - tests/unit/test_binance_feed.py
  - tests/unit/test_alphavantage_feed.py
  - tests/unit/test_pipeline_v2.py
autonomous: true
requirements: []
---

# Plan 04: Pipeline Integration & Tests

<objective>
Upgrade the DataPipeline to support multiple feed providers and dual storage (Redis cache + TimescaleDB persistent). Extend resampler for 1W/1M timeframes. Add comprehensive tests for all new components.
</objective>

<must_haves>
- DataPipeline accepts BaseFeed (not hardcoded YFinanceFeed)
- Pipeline stores to both Redis (cache) and TimescaleDB (persistent)
- Resampler handles 4H, 1W, 1M aggregation
- Comprehensive unit tests for all new code
- All 378+ existing tests continue to pass
</must_haves>

<tasks>

<task id="4.1">
<title>Upgrade DataPipeline for multi-feed and dual storage</title>
<read_first>
- src/algoforge/data/pipeline.py
- src/algoforge/data/feeds/base_feed.py
- src/algoforge/data/storage/timescale_store.py
- src/algoforge/data/storage/redis_store.py
</read_first>
<action>
Update src/algoforge/data/pipeline.py:

1. Change type hints from YFinanceFeed/RedisStore to BaseFeed/abstract:
```python
from algoforge.data.feeds.base_feed import BaseFeed
from algoforge.data.storage.redis_store import RedisStore
from algoforge.data.storage.timescale_store import TimescaleStore

class DataPipeline:
    def __init__(
        self,
        feed: BaseFeed,  # Was: YFinanceFeed
        cache: RedisStore,
        store: TimescaleStore | None = None,  # New: persistent storage
        event_bus: EventBus = None,
    ) -> None:
        self._feed = feed
        self._cache = cache  # renamed from _store
        self._store = store  # TimescaleDB (optional for backward compat)
        self._event_bus = event_bus
        ...
```

2. Update initialize() to connect both stores
3. Update store_candle/store_candles to write to BOTH Redis (cache) and TimescaleDB (persist)
4. Update backfill() to store in TimescaleDB and add extended timeframes
5. Update health_check() to check both stores
6. Keep backward compatibility — if store=None, only use cache (Redis)

Update resampler targets in backfill():
```python
targets = [Timeframe.M5, Timeframe.M15, Timeframe.H1, Timeframe.H4, Timeframe.D1]
```
</action>
<acceptance_criteria>
- pipeline.py __init__ accepts `feed: BaseFeed`
- pipeline.py __init__ accepts `store: TimescaleStore | None`
- pipeline.py initialize() calls store.connect() when store is not None
- pipeline.py store methods write to both cache and store
- pipeline.py health_check returns status for both stores
- `python -c "from algoforge.data.pipeline import DataPipeline"` exits 0
</acceptance_criteria>
</task>

<task id="4.2">
<title>Extend Resampler for 4H, 1W, 1M timeframes</title>
<read_first>
- src/algoforge/data/processors/resampler.py
</read_first>
<action>
Update src/algoforge/data/processors/resampler.py:

1. Add H4 to the resampling targets — aggregate from H1 candles
2. Add W1 resampling — aggregate from D1 candles (Monday-Friday or 7-day for crypto)
3. Add MO1 resampling — aggregate from D1 candles (calendar month boundaries)
4. Ensure resample_to_all() can handle the full chain: 1m → 5m → 15m → 30m → 1h → 4h → 1d → 1w → 1mo

The resampler should use pandas resample with proper OHLCV aggregation:
- open: first
- high: max
- low: min
- close: last
- volume: sum
</action>
<acceptance_criteria>
- resampler.py can resample to Timeframe.H4
- resampler.py can resample to Timeframe.W1
- resampler.py can resample to Timeframe.MO1
- resample_to_all accepts all 9 Timeframe enum values
</acceptance_criteria>
</task>

<task id="4.3">
<title>Write comprehensive unit tests</title>
<read_first>
- tests/unit/test_config.py
- tests/unit/test_pipeline.py
- tests/unit/test_data.py
</read_first>
<action>
Create new test files:

1. tests/unit/test_config_v2.py:
   - Test TimescaleDBConfig defaults
   - Test BinanceConfig defaults
   - Test AlphaVantageConfig defaults
   - Test Settings loads all new sections
   - Test MarketConfig loader reads market YAML files
   - Test timeframe mode config loading

2. tests/unit/test_timescale_store.py:
   - Test TimescaleStore instantiation
   - Test to_timescale_row() serialization
   - Test from_timescale_row() deserialization
   - Test store_candle/store_candles with mocked asyncpg pool

3. tests/unit/test_feed_factory.py:
   - Test create_feed("yfinance") returns YFinanceFeed
   - Test create_feed("binance") returns BinanceFeed
   - Test create_feed("alphavantage") returns AlphaVantageFeed
   - Test create_feed("invalid") raises ValueError

4. tests/unit/test_binance_feed.py:
   - Test BinanceFeed instantiation
   - Test TIMEFRAME_MAP covers all 9 timeframes
   - Test _normalize response conversion to OHLCV

5. tests/unit/test_alphavantage_feed.py:
   - Test AlphaVantageFeed instantiation
   - Test symbol format conversion (EURUSD → EUR/USD)
   - Test volume defaults to 0.0 for forex

6. tests/unit/test_pipeline_v2.py:
   - Test DataPipeline accepts BaseFeed
   - Test dual storage (cache + store)
   - Test pipeline works with store=None (backward compat)
   - Test extended resampling (H4, W1, MO1)

All tests use mocks/fixtures — no real database or API connections needed.
</action>
<acceptance_criteria>
- `pytest tests/unit/test_config_v2.py -v` exits 0
- `pytest tests/unit/test_timescale_store.py -v` exits 0
- `pytest tests/unit/test_feed_factory.py -v` exits 0
- `pytest tests/unit/test_binance_feed.py -v` exits 0
- `pytest tests/unit/test_alphavantage_feed.py -v` exits 0
- `pytest tests/unit/test_pipeline_v2.py -v` exits 0
- `pytest tests/ -x` exits 0 (ALL tests pass including existing 378)
- New test count >= 30
</acceptance_criteria>
</task>

</tasks>

<verification>
- `pytest tests/ -x -q` exits 0 with 400+ tests passing
- `python -c "from algoforge.data.pipeline import DataPipeline; from algoforge.data.feeds.feed_factory import create_feed; print('OK')"` prints OK
- No import errors for any new module
- All 6 Phase 1 success criteria met:
  1. ✓ System ingests OHLCV (YFinance + Binance + AlphaVantage feeds)
  2. ✓ Multi-timeframe resampling up to 1M
  3. ✓ YAML config per market (4 files)
  4. ✓ Data feed reconnects (tenacity retry)
  5. ✓ Pydantic schemas for all models
  6. ✓ Intraday and swing configs
</verification>
